{
  "name": "veltact-pinchme-hackathon",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:contracts": "npm run build -w packages/contracts",
    "dev": "npm run build:contracts && npm run build -w apps/buyer && npm run dev -w apps/api",
    "dev:api": "npm run dev -w apps/api",
    "demo:reset": "node scripts/demo-reset.mjs",
    "build": "npm run build:contracts && npm run build -w apps/api && npm run build -w apps/buyer",
    "lint": "npm run build:contracts && npm run lint --workspaces --if-present",
    "test": "npm run build:contracts && npm run test --workspaces --if-present",
    "typecheck": "npm run build:contracts && npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
