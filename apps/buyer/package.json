{
  "name": "@veltact/buyer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@veltact/contracts": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
